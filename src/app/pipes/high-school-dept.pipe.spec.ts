import { HighSchoolDeptPipe } from './high-school-dept.pipe';

describe('HighSchoolDeptPipe', () => {
  it('create an instance', () => {
    const pipe = new HighSchoolDeptPipe();
    expect(pipe).toBeTruthy();
  });
});
