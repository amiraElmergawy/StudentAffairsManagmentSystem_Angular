import { ConvertIdToNamePipe } from './convert-id-to-name.pipe';

describe('ConvertIdToNamePipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertIdToNamePipe();
    expect(pipe).toBeTruthy();
  });
});
