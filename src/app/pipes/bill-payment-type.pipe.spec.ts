import { BillPaymentTypePipe } from './bill-payment-type.pipe';

describe('BillPaymentTypePipe', () => {
  it('create an instance', () => {
    const pipe = new BillPaymentTypePipe();
    expect(pipe).toBeTruthy();
  });
});
