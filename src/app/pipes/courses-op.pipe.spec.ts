import { CoursesOpPipe } from './courses-op.pipe';

describe('CoursesOpPipe', () => {
  it('create an instance', () => {
    const pipe = new CoursesOpPipe();
    expect(pipe).toBeTruthy();
  });
});
