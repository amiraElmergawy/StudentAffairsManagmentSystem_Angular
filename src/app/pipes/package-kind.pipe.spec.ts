import { PackageKindPipe } from './package-kind.pipe';

describe('PackageKindPipe', () => {
  it('create an instance', () => {
    const pipe = new PackageKindPipe();
    expect(pipe).toBeTruthy();
  });
});
