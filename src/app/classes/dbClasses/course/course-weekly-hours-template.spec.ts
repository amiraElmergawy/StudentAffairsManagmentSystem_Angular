import { CourseWeeklyHoursTemplate } from './course-weekly-hours-template';

describe('CourseWeeklyHoursTemplate', () => {
  it('should create an instance', () => {
    expect(new CourseWeeklyHoursTemplate()).toBeTruthy();
  });
});
