import { StudentSpecialRegistration } from './student-special-registration';

describe('StudentSpecialRegistration', () => {
  it('should create an instance', () => {
    expect(new StudentSpecialRegistration()).toBeTruthy();
  });
});
