import { StudentHighSchool } from './student-high-school';

describe('StudentHighSchool', () => {
  it('should create an instance', () => {
    expect(new StudentHighSchool()).toBeTruthy();
  });
});
