import { StudentAdditionalInfo } from './student-additional-info';

describe('StudentAdditionalInfo', () => {
  it('should create an instance', () => {
    expect(new StudentAdditionalInfo()).toBeTruthy();
  });
});
